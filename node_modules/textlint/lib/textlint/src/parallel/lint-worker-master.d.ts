import { LintWorkerResults } from "./lint-worker";
import { Config } from "../config/config";
export interface lintParallelOptions<T extends "lint" | "fix"> {
    type: T;
    config: Config;
    concurrency?: number;
}
export declare const lintParallel: <T extends "lint" | "fix">(files: string[], options: lintParallelOptions<T>) => Promise<LintWorkerResults<T>>;
